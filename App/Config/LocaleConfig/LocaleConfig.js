import { LocaleConfig } from 'react-native-calendars';

LocaleConfig.locales[languageCode] = {
  dayNamesShort [7],
  dayNamesShort [7],
  monthNames: [12],
  monthNamesShort [12],
};

export LocaleConfig;
