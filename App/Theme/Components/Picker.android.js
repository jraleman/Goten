import variables from './../variables';

export default (variable = variables) => {
  const pickerTheme = {
    '.note': {
      color: '#8F8E95'
    },
    width: 90,
    marginRight: -4
  };

  return pickerTheme;
};
