import login as loginData from './login';
import user as userData from './user';
import notification as notificationData from './notification';
import meeting as meetingData from './meeting';

const fixtures = {
  login: loginData,
  user: userData,
  notification: notificationData,
  meeting: meetingData
};

export default fixtures;
